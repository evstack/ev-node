import{_ as a,c as t,ag as i,o as s}from"./chunks/framework.DlgP5dhN.js";const m=JSON.parse('{"title":"DA","description":"","frontmatter":{"head":[["meta",{"name":"og:title","content":"DA | Evolve"},{"name":"og:description","content":false}]]},"headers":[],"relativePath":"learn/specs/da.md","filePath":"learn/specs/da.md","lastUpdated":1765840739000}'),o={name:"learn/specs/da.md"};function r(n,e,l,c,d,p){return s(),t("div",null,e[0]||(e[0]=[i('<h1 id="da" tabindex="-1">DA <a class="header-anchor" href="#da" aria-label="Permalink to &quot;DA&quot;">​</a></h1><p>Evolve provides a generic <a href="https://github.com/evstack/ev-node/blob/main/block/public.go" target="_blank" rel="noreferrer">data availability interface</a> for modular blockchains. Any DA that implements this interface can be used with Evolve.</p><h2 id="details" tabindex="-1">Details <a class="header-anchor" href="#details" aria-label="Permalink to &quot;Details&quot;">​</a></h2><p><code>Client</code> can connect via JSON-RPC transports using Evolve&#39;s <a href="https://github.com/evstack/ev-node/tree/main/pkg/da/jsonrpc" target="_blank" rel="noreferrer">jsonrpc</a> implementations. The connection can be configured using the following cli flags:</p><ul><li><code>--rollkit.da.address</code>: url address of the DA service (default: &quot;grpc://localhost:26650&quot;)</li><li><code>--rollkit.da.auth_token</code>: authentication token of the DA service</li><li><code>--rollkit.da.namespace</code>: namespace to use when submitting blobs to the DA service (deprecated)</li><li><code>--rollkit.da.header_namespace</code>: namespace to use when submitting headers to the DA service</li><li><code>--rollkit.da.data_namespace</code>: namespace to use when submitting data to the DA service</li></ul><p>The Submitter component now submits headers and data separately to the DA layer using different namespaces:</p><ul><li><strong>Headers</strong>: Submitted to the namespace specified by <code>--rollkit.da.header_namespace</code> (or falls back to <code>--rollkit.da.namespace</code> if not set)</li><li><strong>Data</strong>: Submitted to the namespace specified by <code>--rollkit.da.data_namespace</code> (or falls back to <code>--rollkit.da.namespace</code> if not set)</li></ul><p>Each submission first encodes the headers or data using protobuf (the encoded data are called blobs) and invokes the <code>Submit</code> method on the underlying DA implementation with the appropriate namespace. On successful submission (<code>StatusSuccess</code>), the DA block height which included the blobs is returned.</p><p>To make sure that the serialised blocks don&#39;t exceed the underlying DA&#39;s blob limits, it fetches the blob size limit by calling <code>Config</code> which returns the limit as <code>uint64</code> bytes, then includes serialised blocks until the limit is reached. If the limit is reached, it submits the partial set and returns the count of successfully submitted blocks as <code>SubmittedCount</code>. The caller should retry with the remaining blocks until all the blocks are submitted. If the first block itself is over the limit, it throws an error.</p><p>The <code>Submit</code> call may result in an error (<code>StatusError</code>) based on the underlying DA implementations on following scenarios:</p><ul><li>the total blobs size exceeds the underlying DA&#39;s limits (includes empty blobs)</li><li>the implementation specific failures, e.g., for <a href="https://github.com/evstack/ev-node/tree/main/pkg/da/jsonrpc" target="_blank" rel="noreferrer">celestia-da-json-rpc</a>, invalid namespace, unable to create the commitment or proof, setting low gas price, etc, could return error.</li></ul><p>The retrieval process now supports both legacy single-namespace mode and separate namespace mode:</p><ol><li><p><strong>Legacy Mode Support</strong>: For backward compatibility, the system first attempts to retrieve from the legacy namespace if migration has not been completed.</p></li><li><p><strong>Separate Namespace Retrieval</strong>: The system retrieves headers and data separately:</p><ul><li>Headers are retrieved from the <code>HeaderNamespace</code></li><li>Data is retrieved from the <code>DataNamespace</code></li><li>Results from both namespaces are combined</li></ul></li><li><p><strong>Namespace Migration</strong>: The system automatically detects and tracks namespace migration:</p><ul><li>When data is found in new namespaces, migration is marked as complete</li><li>Migration state is persisted to optimize future retrievals</li><li>Once migration is complete, legacy namespace checks are skipped</li></ul></li></ol><p>If there are no blocks available for a given DA height in any namespace, <code>StatusNotFound</code> is returned (which is not an error case). The retrieved blobs are converted back to headers and data, then combined into complete blocks for processing.</p><p>Both header/data submission and retrieval operations may be unsuccessful if the DA node and the DA blockchain that the DA implementation is using have failures. For example, failures such as, DA mempool is full, DA submit transaction is nonce clashing with other transaction from the DA submitter account, DA node is not synced, etc.</p><h2 id="namespace-separation-benefits" tabindex="-1">Namespace Separation Benefits <a class="header-anchor" href="#namespace-separation-benefits" aria-label="Permalink to &quot;Namespace Separation Benefits&quot;">​</a></h2><p>The separation of headers and data into different namespaces provides several advantages:</p><ul><li><strong>Improved Scalability</strong>: Headers and data can be processed independently, allowing for more efficient resource utilization</li><li><strong>Flexible Data Availability</strong>: Different availability guarantees can be applied to headers vs data</li><li><strong>Optimized Retrieval</strong>: Clients can retrieve only the data they need (e.g., light clients may only need headers)</li><li><strong>Backward Compatibility</strong>: The system maintains support for legacy single-namespace deployments while enabling gradual migration</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><p>[1] <a href="https://github.com/evstack/ev-node/blob/main/block/public.go" target="_blank" rel="noreferrer">da-interface</a></p><p>[2] <a href="https://github.com/evstack/ev-node/tree/main/pkg/da/jsonrpc" target="_blank" rel="noreferrer">jsonrpc</a></p>',21)]))}const u=a(o,[["render",r]]);export{m as __pageData,u as default};
